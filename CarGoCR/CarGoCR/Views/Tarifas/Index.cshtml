@model List<CarGoCR.Models.Tarifa>

@{
    ViewData["Title"] = "Clientes";
}
<link rel="stylesheet" href="~/css/Tarifas/Index.css" asp-append-version="true" />


<div class="main">
    <div class="content">
        <div class="page-header-row">
            <div class="page-header">
                <h1>Tarifas</h1>
                <p>Configura precios por peso, categoría y destino</p>
            </div>

            <a asp-action="Create" class="btn-primary">
                + Nueva tarifa
            </a>
        </div>
        <div class="range-cards">

            <div class="range-card">
                <div>
                    <div class="range-label">
                        Envíos nacionales
                    </div>

                    <div class="range-desc">
                        Dentro de Costa Rica
                    </div>

                    <div class="range-amount">
                        @{
                            var minNacional = Model.Any()
                                ? Model.Min(x => x.PrecioNacional)
                                : 0;

                            var maxNacional = Model.Any()
                                ? Model.Max(x => x.PrecioNacional)
                                : 0;
                        }

                        $@minNacional - $@maxNacional
                    </div>

                    <div class="range-sub">
                        Rango actual
                    </div>
                </div>
            </div>

            <div class="range-card">
                <div>
                    <div class="range-label">
                        Envíos internacionales
                    </div>

                    <div class="range-desc">
                        USA, EU y más
                    </div>

                    <div class="range-amount">
                        @{
                            var minInternacional = Model.Any()
                                ? Model.Min(x => x.PrecioInternacional)
                                : 0;

                            var maxInternacional = Model.Any()
                                ? Model.Max(x => x.PrecioInternacional)
                                : 0;
                        }

                        $@minInternacional - $@maxInternacional
                    </div>

                    <div class="range-sub">
                        Rango actual
                    </div>
                </div>
            </div>

        </div>

        <div class="table-card">
            <table class="data-table">

                <thead>
                    <tr>
                        <th>Categoría</th>
                        <th>Rango de peso</th>
                        <th>Nacional</th>
                        <th>Internacional</th>
                        <th>Acciones</th>
                    </tr>
                </thead>

                <tbody>

                    @if (Model.Any())
                    {
                        @foreach (var tarifa in Model)
                        {
                            <tr>

                                <td class="td-name">
                                    @tarifa.Categoria
                                </td>

                                <td class="td-range">
                                    @tarifa.PesoMinimo kg - @tarifa.PesoMaximo kg
                                </td>

                                <td class="td-price">

                                    @if (tarifa.RequiereCotizacion)
                                    {
                                        <span class="td-cotizar">
                                            Cotizar
                                        </span>
                                    }
                                    else
                                    {
                                        <span>
                                            $@tarifa.PrecioNacional
                                        </span>
                                    }

                                </td>

                                <td class="td-price">

                                    @if (tarifa.RequiereCotizacion)
                                    {
                                        <span class="td-cotizar">
                                            Cotizar
                                        </span>
                                    }
                                    else
                                    {
                                        <span>
                                            $@tarifa.PrecioInternacional
                                        </span>
                                    }

                                </td>

                                <td>
                                    <div class="actions">

                                        <a asp-action="Details"
                                           asp-route-id="@tarifa.Id"
                                           class="action-btn details">
                                            Detalles
                                        </a>

                                        <a asp-action="Edit"
                                           asp-route-id="@tarifa.Id"
                                           class="action-btn edit">
                                            Editar
                                        </a>

                                        <a asp-action="Delete"
                                           asp-route-id="@tarifa.Id"
                                           class="action-btn delete">
                                            Eliminar
                                        </a>

                                    </div>
                                </td>

                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" style="text-align:center;padding:2rem;color:var(--text-secondary);">
                                No hay tarifas registradas
                            </td>
                        </tr>
                    }

                </tbody>

            </table>
        </div>
    </div>
</div>